services:
  - type: web
    name: omniq-seller-service
    env: node
    plan: starter
    buildCommand: pnpm install && pnpm --filter @omniq/seller-service build
    startCommand: pnpm --filter @omniq/seller-service dev
    healthCheckPath: /health
