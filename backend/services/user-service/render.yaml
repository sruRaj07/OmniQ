services:
  - type: web
    name: omniq-user-service
    env: node
    plan: starter
    buildCommand: pnpm install && pnpm --filter @omniq/user-service build
    startCommand: pnpm --filter @omniq/user-service dev
    healthCheckPath: /health
