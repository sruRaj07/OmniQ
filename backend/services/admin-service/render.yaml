services:
  - type: web
    name: omniq-admin-service
    env: node
    plan: starter
    buildCommand: pnpm install && pnpm --filter @omniq/admin-service build
    startCommand: pnpm --filter @omniq/admin-service dev
    healthCheckPath: /health
