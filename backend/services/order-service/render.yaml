services:
  - type: web
    name: omniq-order-service
    env: node
    plan: starter
    buildCommand: pnpm install && pnpm --filter @omniq/order-service build
    startCommand: pnpm --filter @omniq/order-service dev
    healthCheckPath: /health
