services:
  - type: web
    name: omniq-location-service
    env: node
    plan: starter
    buildCommand: pnpm install && pnpm --filter @omniq/location-service build
    startCommand: pnpm --filter @omniq/location-service dev
    healthCheckPath: /health
