services:
  - type: web
    name: omniq-api-gateway
    env: node
    plan: starter
    buildCommand: pnpm install && pnpm --filter @omniq/api-gateway build
    startCommand: pnpm --filter @omniq/api-gateway dev
    healthCheckPath: /health
